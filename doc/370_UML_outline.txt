
370 class outline

BattleBots

Model:
  Robot
  GameObserver(interface)
  Game implements GameControl, GameInforProvider
  GameInforProvider(interface)
  GameControl(interface)
  Scout extends Robot
  Sniper extends Robot
  Tank extends Robot
  ScoutAI extends Robot
  SniperAI extends Robot
  TankAI extends Robot

View:
  View extends JFrame
  ViewPanel extends JPanel
  ImageCache
  GraphicsPanel extends JPanel
  MainMenu extends ViewPanel
  HelpPanel extends ViewPanel
  GameProperties extends ViewPanel
  TeamSelectionPanel extends ViewPanel
  GamePanel extends GraphicsPanel
  WinnerPanel extends ViewPanel
  StatsPanel extends ViewPanel
  
Controller:
  Controller implements KeyListener, ActionListener, GameObserver

